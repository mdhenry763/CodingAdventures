using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActivatedFallingObject : MonoBehaviour
{
    /*Rigidbody rb;
    FallingObjects fo;
    // Start is called before the first frame update
    void Start()
    {
        rb = GetComponent<Rigidbody>();
        fo = GetComponent<FallingObjects>();
    }

    // Update is called once per frame
    void Update()
    {
        if (fo.fallingActivate())
        {
            rb.useGravity = true;
        }    
    }*/
}
